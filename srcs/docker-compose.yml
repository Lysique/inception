version: "2.5.1"
 
services:
    nginx:
        container_name: "nginx"
        build: ./requirements/nginx/
        ports:
            - "443:443"
        restart: always

            #depends_on
            #networks
            #volumes
    wordpress:
        container_name: "wordpress"
        build: ./requirements/wordpress/
        ports:
            - "9000:9000"
        restart: always
        #depends_on:
