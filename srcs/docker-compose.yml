version: "2.5.1"
 
services:
    nginx:
        container_name: "nginx"
        build: ./requirements/nginx/
        ports:
            - "443:443"
        restart: unless-stopped

            #depends_on
            #networks
            #volumes
